apache-beam[gcp]==2.53.0
zstandard==0.22.0
objsize>=0.6.1

# Optional: For data manipulation, only include if actually used
pandas==2.2.2

# Required by many data tools, including pandas
numpy==1.26.4

# For working with Google Cloud Storage (GCS) — required if you're reading/writing GCS files
google-cloud-storage==2.16.0



